/** Platform economics and policy constants. */

// Flat platform commission until per-category fees land (admin, Phase 6).
export const DEFAULT_PLATFORM_FEE_PERCENT = 10;

// Buyers may cancel before the order ships; returns allowed within this window.
export const RETURN_WINDOW_DAYS = 7;
